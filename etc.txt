$ git rm -rf --cached foldername

$ git remote remove heroku	// to remove remotes

$ git remote set-url origin new_URL	// change url

Delete remote branch
$ git push origin --delete TestSuitJunit4

Discard changes
$ git checkout -- filepath

Merge 2 or more git repos together :
https://medium.com/altcampus/how-to-merge-two-or-multiple-git-repositories-into-one-9f8a520991f

I also want to learn git.
